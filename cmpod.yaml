apiVersion: v1
kind: Pod
metadata:
  name: mysqlpod-new
  namespace: cinjith
  labels:
    env: dev
    user: cb
spec:
  containers:
  - name: c1
    image: mysql
    envFrom:
    - configMapRef:
        name: cm1
